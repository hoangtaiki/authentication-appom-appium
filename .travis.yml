language: ruby
cache: bundler
os: osx10

rvm:
- 2.4.4

before_install:
  - brew update
  - brew upgrade node
  - npm install -g appium
  - npm install wd
  - gem install bundler
  - bundle install

script:
  - appium &
  - sleep 8
  - cucumber
